


//-------------
// Game state + logic
// ---------
// let playerTurn = 'Player1';
// var numberOfGreyPebbles = 16;








// ------------
// Util-
// ------------


// ------------
// Validation-
// -------------
// (the player cannot take more pebbles than left, so when the 2 pebbles are left, the player cannot take 3, when 1 pebble left, cannot take 2 or 3.)
// ------------
// Events-
// ------------
// 1)Select the number in the option 2)submit the form
function getSelectedValue() {
  var selectedValue = document.getElementById('takeInput').value;
  console.log(selectedValue);
}

function playGame () {
  getSelectedValue();
  //validation
  if (selectedValue > numberOfGreyPebbles) {
    console.log('error:You cannot select more than available')
    return
  } else if
  //one player won
  (selectedValue = numberOfGreyPebbles) {
    console.log(playerTurn + 'has won')
    return
  } else {
  //switch player
  if (playerTurn == 'Player1') {
    playerTurn = 'Player2';
  } else {
    playerTurn = 'Player1'
  }
  //play the game
  var numberOfGreyPebbles = numberOfGreyPebbles - selectedValue;
  renderGame(selectedValue, playerTurn);
  console.info(playerTurn);
}
}

// -------------
// Rendering-
// -------------
// Render the game includes three parts: 1)render the title, 2)render the pebble color image, 3) render the player's turn.
// render the title
var title = document.getElementByClass('title')
var pebbleImage = document.getElementByClass('pebble-container')
var playerState = document.getElementByClass('player-state')

title.innerHTML = renderTitle(selectedValue)
pebbleImage.innerHTML = renderPebbleImage(selectedValue)
playerState.innerHTML = renderPlayerTurn(playerTurn)

function renderTitle (numberOfGreyPebbles) {
  return `<h4 class="title">There are ${numberOfGreyPebbles} pebbles left</h4>` 
}
//render the pebble image, change background color for the selected number of pebbles
function renderPebbleImage (selectedValue) {
  //change the pebble class css background-color by id
  
}

//render player-state banner
function renderPlayerState (playerTurn) {
  //show the current playerTurn
}

function renderGame(selectedValue, playerTurn) {
  renderTitle(selectedValue);
  renderPebbleImage(selectedValue);
  renderPlayerState(playerTurn);
}
//




